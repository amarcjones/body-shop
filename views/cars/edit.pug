extends ../base.pug

block content
  form(action=`/drivers/${car.driver.id}/cars/${car.id}?_method=PATCH`, method="POST")
    p
      input(type="text", name="make", value=car.make, id="make")
    p
      input(type="text", name="model", value=car.model, id="model")
    p
      input(type="number", name="year", value=car.year, id="year")
    p    	
      button Edit a new car

  form(action=`/drivers/${car.driver.id}/cars/${car.id}?_method=DELETE`, method="POST")
    input(type="submit", value="X")